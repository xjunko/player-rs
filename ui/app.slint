import { KKRState } from "./state.slint";
export { KKRState }

import { KKRPlayButton, KKRButton } from "./components.slint";
import { KKRTextButton } from "components.slint";

export component AppWindow inherits Window {
    width: 400px;
    height: 165px;
    title: "Kokoro";

    VerticalLayout {
        width: parent.width;
        height: parent.height;
        spacing: 0px;

        // background
        Rectangle {
            horizontal-stretch: 1;
            background: #181818;
            clip: true;

            // top
            HorizontalLayout {
                padding: 8px;
                spacing: 8px;

                if KKRState.track.cover-path != "": Rectangle {
                    width: 100px;
                    height: 100px;
                    background: white;

                    Image {
                        width: 100%;
                        height: 100%;
                        image-fit: cover;
                        source: KKRState.track.cover;
                    }
                }

                VerticalLayout {
                    horizontal-stretch: 1;
                    spacing: 2px;
                    alignment: center;

                    Text {
                        text: KKRState.track.artist;
                        color: #999;
                        font-size: 13px;
                        overflow: elide;
                    }

                    Text {
                        text: KKRState.track.title;
                        color: white;
                        font-size: 16px;
                        font-weight: 700;
                        overflow: elide;
                    }
                }
            }
        }

        // separator
        Rectangle {
            height: 1px;
            background: #333;
        }

        // bottom control
        Rectangle {
            height: 48px;
            background: #1a1a1a;

            HorizontalLayout {
                padding-left: 16px;
                alignment: start;
                Text {
                    text: KKRState.current_time_text;
                    color: #999;
                    font-size: 13px;
                    vertical-alignment: center;
                }
            }

            HorizontalLayout {
                padding-left: 8px;
                padding-right: 16px;
                alignment: end;
                spacing: 4px;

                KKRTextButton {
                    icon: "<<";
                    clicked => {
                        KKRState.prev();
                    }
                }

                KKRPlayButton {
                    clicked => {
                        KKRState.play();
                    }
                }

                KKRTextButton {
                    icon: ">>";
                    clicked => {
                        KKRState.next();
                    }
                }
            }
        }
    }
}
